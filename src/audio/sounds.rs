/// Default ready check sound embedded in the binary.
pub const READY_CHECK: &[u8] = include_bytes!("../../sounds/ready_check.wav");

/// Default squad ready sound embedded in the binary.
pub const SQUAD_READY: &[u8] = include_bytes!("../../sounds/squad_ready.wav");
